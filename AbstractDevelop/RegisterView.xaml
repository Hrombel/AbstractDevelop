<UserControl x:Class="AbstractDevelop.RegisterView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:AbstractDevelop"
             xmlns:lang="http://gu.se/Localization"
             xmlns:p="clr-namespace:AbstractDevelop.Properties"
             
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <DataGrid AutoGenerateColumns="False">
        <DataGrid.Columns>
            <DataGridTextColumn Header="{lang:Static p:Labels.RegistersColumnID}"/>
            <DataGridTextColumn Header="{lang:Static p:Labels.RegistersColumnValue}" Width="*"/>
        </DataGrid.Columns>
    </DataGrid>
</UserControl>
